<div class="row">
	<% @brands.each do |brand| %>
		<div class="col-xs-1 col-md-6" style="
    padding-right: 0px;
    padding-left: 0px;
">
			<%= link_to image_tag(brand.logo.url(:mini), :class => 'img-fluid'), brand, data: { turbolinks: false} %> 
		</div>
	<%end%>
</div>
	
	
