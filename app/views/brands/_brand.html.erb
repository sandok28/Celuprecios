<div class="row">
	<% @brands.each do |brand| %>
		<div class="col-xs-1 col-md-6" style="padding: 3%;" >
			<%= link_to image_tag(brand.logo.url(:mini)), brand,class=>"img-fluid", data: { turbolinks: false} %> 
		</div>
	<%end%>
</div>
	
	
